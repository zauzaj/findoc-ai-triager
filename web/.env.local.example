# Rails API base URL (no trailing slash)
NEXT_PUBLIC_RAILS_API_URL=http://localhost:3001/api/v1

# Google Maps — enables Static Maps pin preview and Maps Embed iframe
# Required APIs: Maps Static API, Maps Embed API
NEXT_PUBLIC_GOOGLE_MAPS_KEY=
